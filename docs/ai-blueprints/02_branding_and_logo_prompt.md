Please update the branding and layout of our mobile view header to match the new identity:

1. APPLICATION NAME:
Change the app name text throughout the components from "GreenRoute" to "EcoQuest SG" exactly as specified.

2. BRAND LOGO DESIGN:
In the top-left corner of the header, render a styled SVG asset representing the brand logo. The logo should feature a crisp, vibrant green tree emblem encapsulated within a dark rounded badge, exactly matching the styling from the generated mockup images.

3. HEADER LAYOUT POLISH:
Ensure the logo sits perfectly inline with the text title "EcoQuest SG". To the far right of the header, keep an authoritative status indicator reading "SYNCED" with a tiny green circular pulse or sync icon to emphasize our live connection to the LTA Datamall and OneMap API infrastructure.