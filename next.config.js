/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you had any specific configurations (like images or rewrites), put them here
};

module.exports = nextConfig;